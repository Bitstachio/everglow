# Alerting

What in the API is worth waking someone for, and how to tell. Companion to
[`logging-conventions.md`](./logging-conventions.md): that document says how to write a log line, this one says
which lines an alert rule is built on.

---

## 1. Approach

- **Alerts are rules over log events.** The API already writes structured JSON with a stable `event` field
  (conventions §5). Whatever log platform is chosen at deploy time evaluates the rules in §3 against those lines.
  The API holds no alerting code and no vendor SDK, and nothing here depends on the choice of platform.
- **Error tracking is a separate, later decision.** Grouping and triaging unexpected 5xx (stack traces, release
  tagging) is a different tool for a different job. Until it exists, the `request.unhandled_error` rule below is
  the coarse substitute.
- **A rule matches on `event`, never on the message.** Messages are for people and may be reworded. Where one
  event is logged at more than one level, the rule also matches on `level` or on a field; the table says which.
- **The event names are a contract.** Every name in §3 lives in
  [`src/common/logging/alert-events.constants.ts`](../src/common/logging/alert-events.constants.ts), the code that
  logs it references the constant, and a unit test pins the strings. Renaming one means changing the alert rule
  and this document in the same change. The count fields the rules read (`failed`, `abandoned`, `expired.failed`,
  `completed`) are part of the same contract.

### Scheduler heartbeats

A cron job that stops firing logs no error, so there is nothing to match on. Every `@Cron` handler therefore runs
through [`runScheduledJob`](../src/common/scheduling/run-scheduled-job.ts), which writes exactly one line per run:

- `<job>.run_completed` at `info` when the run returned, **including a run that found nothing to do**, with
  `durationMs` and the job's result counts;
- `<job>.run_failed` at `error` with `err` and `durationMs` when it threw.

The alert is on the **absence** of `run_completed` for longer than twice the job's interval. A disabled job logs
neither line, so a heartbeat rule belongs only in an environment where the job is switched on.

| Job                             | Heartbeat event                                 | Interval     | Enabled by                                      |
| ------------------------------- | ----------------------------------------------- | ------------ | ----------------------------------------------- |
| Account deletion reconcile      | `user.account.deletion_reconcile.run_completed` | hourly       | `ACCOUNT_DELETION_RECONCILER_ENABLED=true`      |
| Pending photo cleanup           | `photo.pending_cleanup.run_completed`           | hourly       | on unless `PHOTO_PENDING_CLEANUP_ENABLED=false` |
| Orphaned photo object reconcile | `photo.orphan_reconcile.run_completed`          | daily, 03:00 | `PHOTO_ORPHAN_RECONCILER_ENABLED=true`          |

The names are per job rather than one shared `scheduler.run_completed` with a `job` field: the failure events
already existed as `<job>.run_failed` and must not change, the pair reads as one thing, and the names keep the
`<domain>.<entity>.<action>` shape of every other event. One rule can still cover all jobs by matching the
`.run_failed` suffix.

---

## 2. Severity

- **Page**: someone is locked out, data is not being deleted that must be, or a safety net is gone. Act now.
- **Ticket**: money or quota is leaking, or a job is degraded but still running. Act in working hours.

---

## 3. Events to alert on

| Event                                                                                                                                             | Level                                               | Source                                                                                                      | What it means                                                                                                                                                                   | Condition                                                                                                                                                                                                                             | What to do                                                                                                                                                                                                                                                                                   |
| ------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------- | ----------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `user.account.deletion_abandoned`                                                                                                                 | error                                               | `users/account-deletion-reconciler.service.ts`                                                              | A deletion used up `ACCOUNT_DELETION_MAX_ATTEMPTS` and will not be retried. The person is locked out and their data is still here.                                              | Any occurrence. **Page.**                                                                                                                                                                                                             | Find the cause in that `userId`'s `user.account.deletion_reconcile.failed` lines, fix it, then hand the row back to the job: [`account-deletion.md`](./account-deletion.md) §4, "Recovering an abandoned deletion".                                                                          |
| `user.account.deletion_stuck`                                                                                                                     | warn                                                | `users/account-deletion-reconciler.service.ts`                                                              | A deletion is still pending past `ACCOUNT_DELETION_RECONCILER_STUCK_AFTER_HOURS`. Logged on every run until it finishes or is abandoned.                                        | Any occurrence. **Ticket**; the page comes from `deletion_abandoned`.                                                                                                                                                                 | Read the matching `deletion_reconcile.failed` line for the error. Usually Auth0 or Apple is unreachable and the next run heals it.                                                                                                                                                           |
| `user.account.deletion_reconciler.disabled`                                                                                                       | warn                                                | `users/account-deletion-reconciler.scheduler.ts`                                                            | Logged once at boot when the reconciler is off. A deletion that fails part way is then never finished.                                                                          | Any occurrence in production. **Page.**                                                                                                                                                                                               | Set `ACCOUNT_DELETION_RECONCILER_ENABLED=true` in the environment whose database owns the Auth0 tenant, and redeploy.                                                                                                                                                                        |
| `user.account.apple_revocation_skipped`                                                                                                           | error (`reason` = `unconfigured` or `no_token`)     | `users/apple-identity-revocation.service.ts`                                                                | An Apple user was deleted without their Sign in with Apple token being revoked, which App Store rules require. Points to misconfiguration, so every Apple deletion is affected. | Any occurrence with `level = error`. **Page.** Ignore `reason = identity_gone`, which is a `warn` (see §4).                                                                                                                           | `unconfigured`: set the `APPLE_SIWA_*` variables. `no_token`: give the Auth0 management client `read:user_idp_tokens` and check the Apple connection stores tokens. The affected people must revoke from their Apple ID settings; the `userId`s are in the log lines.                        |
| `user.account.apple_revocation_failed`                                                                                                            | error                                               | `users/apple-identity-revocation.service.ts`                                                                | Apple refused the revocation with a non-retryable error. The deletion went ahead without it.                                                                                    | Any occurrence. **Page**: a refusal is usually a wrong client id, key, or team id.                                                                                                                                                    | Read `err` for Apple's error code, check the `APPLE_SIWA_*` values against the Apple developer account.                                                                                                                                                                                      |
| `*.run_failed`: `user.account.deletion_reconcile.run_failed`, `photo.pending_cleanup.run_failed`, `photo.orphan_reconcile.run_failed`             | error                                               | `common/scheduling/run-scheduled-job.ts`                                                                    | A whole scheduler run threw (database or S3 listing down), as opposed to one item in it failing.                                                                                | Two consecutive runs of the same job. **Page** for the account deletion reconciler, **ticket** for the photo jobs. One failure is left alone: the next run retries.                                                                   | Read `err`. Check database and S3 health, then let the next run confirm recovery with a `run_completed`.                                                                                                                                                                                     |
| `*.run_completed`: `user.account.deletion_reconcile.run_completed`, `photo.pending_cleanup.run_completed`, `photo.orphan_reconcile.run_completed` | info                                                | `common/scheduling/run-scheduled-job.ts`                                                                    | The heartbeat. Its absence means the job is not firing at all.                                                                                                                  | **No line for more than twice the interval** (2 h for the hourly jobs, 48 h for the daily one) where the job is enabled. Same severities as `run_failed`.                                                                             | Check the process is up and the job's flag is still set. A `run_failed` in the window means the job fires but fails: follow that row instead.                                                                                                                                                |
| `*.run_completed`, count fields                                                                                                                   | info                                                | `common/scheduling/run-scheduled-job.ts`                                                                    | The job ran but could not finish some items. Per-item detail is in the `error` lines listed in §4.                                                                              | `failed > 0` (pending cleanup: also `expired.failed > 0`) on three consecutive runs, or `completed = false` on three consecutive orphan reconciles. **Ticket.** `abandoned > 0` needs no rule of its own: `deletion_abandoned` pages. | Pending cleanup: slots that cannot be released keep holding the uploader's quota, so check S3 and database errors in `photo.pending_cleanup.failed` / `.expired_failed`. Orphan reconcile: check `delete_failed`, or raise `PHOTO_ORPHAN_RECONCILER_BATCH_SIZE` if the backlog never clears. |
| `user.account.photos_purged`, `event.photos.purged`                                                                                               | error when `failed > 0` (info otherwise)            | `photos/photo-purge.service.ts`; names passed in by `users/users.service.ts` and `events/events.service.ts` | Rows are gone but some S3 objects could not be deleted. They are billed orphans until the orphan reconciler takes them.                                                         | `level = error` (equivalently `failed > 0`). **Ticket** where the orphan reconciler is enabled and its heartbeat is healthy, **page** where it is not, since nothing will retry.                                                      | Confirm the next `photo.orphan_reconcile.run_completed` shows the objects deleted. If it is off, enable it or delete the keys by hand.                                                                                                                                                       |
| `photo.storage.reservation_conflict`                                                                                                              | warn                                                | `photos/photo-storage.service.ts`                                                                           | An upload reservation lost a serializable-transaction conflict. A few are normal under concurrency; `willRetry = false` means the client got a 409.                             | More than 20 in 10 minutes, or any 5 with `willRetry = false` in 10 minutes. **Ticket.** Tune once there is real traffic.                                                                                                             | One `userId`: a client is firing parallel batches, take it to the mobile team. Many users: contention on the quota transaction, see [`photos-architecture.md`](./photos-architecture.md) §9.                                                                                                 |
| `photo.upload_slots.presign_failed`                                                                                                               | info (slots released) or error (release failed too) | `photos/photos.service.ts`                                                                                  | S3 could not presign an upload batch. The caller got a 5xx and no upload URL.                                                                                                   | 3 or more in 5 minutes at any level. **Page**: nobody can upload.                                                                                                                                                                     | Check S3 credentials, region, and bucket config; the `s3 getPresignedUploadUrl failed` line from `S3Service` carries the error.                                                                                                                                                              |
| `photo.upload_slots.presign_failed`, `photo.upload_slots.rejected`                                                                                | error                                               | `photos/photos.service.ts`                                                                                  | Releasing upload slots failed, so the rows hold quota until the hourly cleanup reclaims them.                                                                                   | Any 3 with `level = error` in 1 hour. **Ticket.**                                                                                                                                                                                     | Read `err` (a database error). Confirm the pending cleanup heartbeat is healthy, since it is now the only thing returning that quota.                                                                                                                                                        |
| `rate_limit.exceeded`                                                                                                                             | warn                                                | `common/rate-limit/rate-limit-rejection.logger.ts`                                                          | A caller exhausted a rate limit tier. One line per blocked bucket per window, so the count is buckets, not requests; `keyedBy`, `userId` and `route` say who and where.         | More than 20 in 10 minutes, or 3 on the `sensitive` tier from distinct `userId`s in 10 minutes. **Ticket.** Tune once there is real traffic.                                                                                          | One user or route: a client looping on a request, take it to the mobile team. Many addresses on `join`: invitation guessing, lower `RATE_LIMIT_SENSITIVE_LIMIT` and rotate exposed links. `default` rejections at a venue: one NAT, raise `RATE_LIMIT_DEFAULT_LIMIT`.                        |
| `request.unhandled_error`                                                                                                                         | error                                               | `common/filters/all-exceptions.filter.ts`                                                                   | A non-HTTP exception escaped to the global filter: an unexpected 5xx.                                                                                                           | 5 or more in 5 minutes. **Page.** A lone one is a ticket.                                                                                                                                                                             | Use `reqId` to pull the request's other lines and read the stack in `err`. This is the rule error tracking replaces.                                                                                                                                                                         |

---

## 4. Events that must not page anyone

Expected outcomes, audit records, and per-item detail whose alert lives on another line. Listed so that nobody
builds a rule on them and nobody wonders whether one is missing.

| Event                                                                                                                                                                 | Level | Why it does not alert                                                                                                                                                       |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `user.account.deletion_reconcile.failed`                                                                                                                              | error | One attempt at one deletion failed and will be retried next hour. The alerts are `deletion_stuck` and `deletion_abandoned`; this line is where they send you for the cause. |
| `photo.pending_cleanup.failed`, `photo.pending_cleanup.expired_failed`, `photo.orphan_reconcile.delete_failed`                                                        | error | One item in a job run failed and is retried on the next run. The alert is on the heartbeat's `failed` counts, which separates "sustained" from "once".                      |
| `user.account.apple_revocation_skipped` with `reason = identity_gone`                                                                                                 | warn  | A resumed deletion found the Auth0 user already gone; the token went with it. Nothing is left to revoke.                                                                    |
| `photo.upload_rejected.object_retained`                                                                                                                               | warn  | A mismatched upload could not be deleted from S3; its row stays and the hourly cleanup retries it.                                                                          |
| `user.account.deletion_reconcile.completed`, `photo.pending_cleanup.completed`, `photo.pending_cleanup.expired_completed`, `photo.orphan_reconcile.completed`         | info  | The services' own audit summaries. The heartbeat carries the same counts and, unlike some of these, is written on idle runs too.                                            |
| `photo.orphan_reconcile.deleted`                                                                                                                                      | info  | Audit record of one orphan removed. That is the job working.                                                                                                                |
| `user.account.deletion_started`, `.deletion_prepared`, `.apple_token_revoked`, `.auth0_deleted`, `.deleted`                                                           | info  | The steps of a healthy account deletion, kept for audit.                                                                                                                    |
| `user.resolve.rejected_deletion_in_progress`, `user.resolve.rejected_tombstone`                                                                                       | info  | A deleted or deleting account tried to sign in and was refused, as designed.                                                                                                |
| `photo.upload_slots.rejected` at `info`                                                                                                                               | info  | A confirm found missing or mismatched uploads and released their slots: a client problem, handled.                                                                          |
| `user.*`, `event.*`, `photo.*` business events (`user.provisioned`, `event.created`, `photo.uploads_confirmed`, `photo.deleted`, `user.storage_limit.increased`, ...) | info  | Normal product activity. Material for dashboards, not alerts.                                                                                                               |
| Ingress request logs for 4xx                                                                                                                                          | warn  | Expected client errors (conventions §4).                                                                                                                                    |

---

## 5. Adding or changing an alert

1. Add the event name to `ALERT_EVENTS` and to the pinned test next to it, and log it through the constant.
2. Add a row to §3 with a condition and an action. An alert nobody knows how to act on is noise.
3. Never rename an event in §3 without changing the platform's rule in the same release.
