# Terraform working files. The lock file (.terraform.lock.hcl) IS committed.
.terraform/
*.tfstate
*.tfstate.*
crash.log

# Local variable values; alert_email must not be committed (public repo).
*.tfvars
!*.tfvars.example
