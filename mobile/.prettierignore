# artifacts
build
coverage
.maestro/results

# pnpm rewrites the lockfile in its own style on every install, so formatting it
# here only causes churn between pnpm and Prettier.
pnpm-lock.yaml

# third-party agent skills (installed/updated via skills-lock.json)
.agents/skills