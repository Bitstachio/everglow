import { View } from "react-native";

// The native stack header supplies the title and Back control for this stub.
const AccountSettingsScreen = () => <View style={{ flex: 1 }} />;

export default AccountSettingsScreen;
